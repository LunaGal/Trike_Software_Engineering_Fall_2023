#include <iostream>
#include "game.h"

class Game {
	char player_name;
	Board curr_board;
	void request_starting_pos() {
		// Requests starting position from player and places it on the board
		// Uses move_neutral to place it
	}
}